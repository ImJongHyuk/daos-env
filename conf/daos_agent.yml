name: daos_server
port: 10001
log_file: /tmp/daos_agent.log
control_log_mask: DEBUG

#runtime_dir: "/opt/daos/daos_agent"

transport_config:
  allow_insecure: true

access_points:
  - "storage01"
  - "storage02"
  - "storage03"
